# Source Code

This folder is for any additional source files, helper scripts, or alternate
versions of the firmware (e.g., a Python script for serial monitoring/logging).

The main Arduino sketch lives in `firmware/Hello_Buddy/Hello_Buddy.ino`.
